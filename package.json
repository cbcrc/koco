{
  "name": "koco",
  "version": "1.2.0",
  "description": "knockout components with routing",
  "main": "src/koco.js",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cbcrc/koco.git"
  },
  "keywords": [
    "koco",
    "routing",
    "knockout",
    "components"
  ],
  "author": "Radio-Canada",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/cbcrc/koco/issues"
  },
  "homepage": "https://github.com/cbcrc/koco#readme",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
